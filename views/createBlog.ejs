<html>
  <!-- use &gt%- so html renders as html 
    and not as escape strings as is the case with  &gt%=   -->
    <%- include("./partials/header.ejs")%>
  <body>
       <!-- Use - instead of = in ejs tag so var not special escaped etc -->
       <%- include("./partials/navigation.ejs");  %>

  

    <div class="createBlog content">
      <form>
        <label for="title">Blog Title:</label>
        <input type="text" id="title" placeholder = "<%= atitle %>"  required >
         <label for="snippet">Blog snippet:</label>
        <input type="text" id="snippet" placeholder = "<%= asnippet %>" required />
        <label for="body">Blog body:</label>
        <textarea id="body" required placeholder="<%= abody %>" ></textarea>
        <button>Submit</button>
      </form>
    </div>
    <%- include("./partials/footer.ejs"); %>
  </body>
</html>
